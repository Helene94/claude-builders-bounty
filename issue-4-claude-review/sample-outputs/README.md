# Sample Outputs

This folder is reserved for the two real GitHub PR review outputs required by the bounty.

Capture them with commands like:

```bash
node src/cli.mjs \
  --pr https://github.com/owner/repo/pull/123 \
  --out sample-outputs/pr-123.md
```

```bash
node src/cli.mjs \
  --pr https://github.com/owner/repo/pull/456 \
  --out sample-outputs/pr-456.md
```

Do not submit placeholder content as final bounty evidence.
